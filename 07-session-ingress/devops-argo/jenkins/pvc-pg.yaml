apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-pg
  namespace: discovery
spec:
  storageClassName: "hostpath"
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi